{
  "name": "p3-ae1-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Accesos a los comandos del módulo M9 desde la raíz del repositorio.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "npm ci --prefix M9-ReservasProgramadas",
    "dev": "npm --prefix M9-ReservasProgramadas run dev",
    "build": "npm --prefix M9-ReservasProgramadas run build",
    "start": "npm --prefix M9-ReservasProgramadas start",
    "local:up": "npm --prefix M9-ReservasProgramadas run local:up",
    "local:down": "npm --prefix M9-ReservasProgramadas run local:down",
    "local:clean": "npm --prefix M9-ReservasProgramadas run local:clean",
    "test": "npm --prefix M9-ReservasProgramadas test",
    "test:coverage": "npm --prefix M9-ReservasProgramadas run test:coverage",
    "test:e2e": "npm --prefix M9-ReservasProgramadas run test:e2e",
    "test:e2e:only": "npm --prefix M9-ReservasProgramadas run test:e2e:only",
    "test:watch": "npm --prefix M9-ReservasProgramadas run test:watch",
    "lint": "npm --prefix M9-ReservasProgramadas run lint",
    "lint:fix": "npm --prefix M9-ReservasProgramadas run lint:fix",
    "format": "npm --prefix M9-ReservasProgramadas run format",
    "format:check": "npm --prefix M9-ReservasProgramadas run format:check",
    "typecheck": "npm --prefix M9-ReservasProgramadas run typecheck",
    "verify": "npm --prefix M9-ReservasProgramadas run verify"
  }
}
